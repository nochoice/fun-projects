const translations = {
    y: 'year',
    d: 'day',
    h: 'hour',
    m: 'minute',
    s: 'second'
}

const formatDuration = (seconds) => {
    const format = [];

    return seconds;
}

console.log(formatDuration(32));