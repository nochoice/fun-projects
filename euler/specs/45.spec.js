const assert = require('assert');
const app = require('../45-triangular-pentagonal-hexa');

describe.only('Discriminant', function() {

  // it('it should multiplacte simple', function() {
  //   assert.equal(app.discriminant(1, 2, 1), 2);
  // })

});

